#include <stdio>

using namespace std;

int main(int argc, char const *argv[]) {

  while (1) {
    int consumo_total, diferenca_valores;

    scanf("%d %d", &consumo_total, &diferenca_valores);

    if (consumo_total == 0 && diferenca_valores == 0)
      break;

    
  }
  return 0;
}
